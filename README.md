# Tdd-es-par
## RED de la funcion_es_par
### *Dado que existe una funcion es_par, cuando llamo a la funcion es_par(4), entonces debe de retornar True porque 4 es par

### *Dado que existe una funcion es_par, cuando llamo a la funcion es_par(3) entonces debe de retornar False porque 3 es impar

### *Dado que existe una funcion es_par, cuando llamo a la funcion es_par(-2) entonces debe de retornar True porque -2 es par

### *Dado que existe una funcion es_par, cuando llamo a la funcion es_par(-1) entonces debe de retornar False porque -1 es impar

### *Dado que existe una funcion es_par, cuando llamo a la funcion es_par(-10) entonces debe de retornar True porque -10 es par

# RED de la funcion es_multiplo_de

### *Dado que existe una funcion es_multiplo_de, cuando llamo a la funcion es_multiplo_de(6,2) entonces debe de retornar True porque 6 es multiplo de 2

### *Dado que existe una funcion es_multiplo_de, cuando llamo a la funcion es_multiplo_de(-3,6) entonces debe de retornar False porque -3 no es multiplo de 6
